
Процедура ПередНачаломРаботыСистемы(Отказ)
	//Если Не Вспомогательный.НастройкиЗаполнены() Тогда 
	//	ОткрытьФорму("ОбщаяФорма.ФормаКонстант");
	//КонецЕсли;
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
	PushКлиент.ПодключитьОбработчикУведомленийИзОбщегоМодуля();
	Если Вспомогательный.НастройкиЗаполнены() Тогда 
		PushКлиент.ОбновитьИдентификаторПодписчика();
	КонецЕсли;
КонецПроцедуры
